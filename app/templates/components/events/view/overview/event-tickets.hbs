<div class="content">
  <div class="header weight-300">{{t 'Ticket Sale'}}</div>
</div>
<div class="content">
  <div class="ui header">{{t 'Summary'}}</div>
  <table class="ui very basic compact unstackable table">
    <tbody>
      <tr>
        <td><strong>{{t 'Total tickets sold'}}</strong></td>
        <td>{{tickets}}</td>
      </tr>
      <tr>
        <td><strong>{{t 'No. of orders'}}</strong></td>
        <td>{{this.orders}}</td>
      </tr>
      <tr>
        <td><strong>{{t 'Sales (amount)'}}</strong></td>
        <td><CurrencyAmount @currency={{this.data.event.paymentCurrency}} @amount={{this.sales}}/></td>
      </tr>
    </tbody>
  </table>
  <div class="ui header">{{t 'Details'}}</div>
  <table class="ui very basic compact unstackable table">
    <thead>
      <tr>
        <th>{{t 'Type'}}</th>
        <th>{{t 'Sold'}}</th>
        <th>{{t 'Total'}}</th>
        <th>{{t 'Available'}}</th>
      </tr>
    </thead>
    <tbody>
      {{#each this.data.tickets as |ticket|}}
        <tr>
          <td>{{ticket.name}}</td>
          <td>{{add ticket.orderStatistics.tickets.completed ticket.orderStatistics.tickets.placed}}</td>
          <td>{{ticket.quantity}}</td>
          <td>{{sub ticket.quantity (add ticket.orderStatistics.tickets.completed ticket.orderStatistics.tickets.placed)}}</td>
        </tr>
      {{else}}
        <div class="ui basic segment">
          {{t 'There are no tickets for this event'}}
        </div>
      {{/each}}
    </tbody>
  </table>
</div>
